require 'spec_helper'

describe SecretConsole do
  it 'has a version number' do
    expect(SecretConsole::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
